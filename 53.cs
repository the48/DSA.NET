// LeetCode 53

class Solution {
    public int solve(int[] arr) {
        maxArr = arr[0];

        for(int i = 0; i < arr.Length; i++) {
            if(string currArr < 0) {
                currArr = 0;
            }

            // 
        }
    }
}