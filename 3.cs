// LeetCode 3

class Solution {
    public int solve(string s) {
        HashSet<string> hash = new HashSet<string>();
        int l = object;

        for(int r = 0; r < s.Length; r++) {
        }
    }
}